# Proxy API requests to ZenQuotes to avoid CORS issues
/api/*  https://zenquotes.io/api/:splat  200

# Catch-all fallback for React Router (must be placed after specific rules)
/*      /index.html   200